<%= form_for(@teacherassign) do |f| %>
  <% if @teacherassign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacherassign.errors.count, "error") %> prohibited this teacherassign from being saved:</h2>

      <ul>
      <% @teacherassign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :faculty_id %><br>
    <%= f.text_field :faculty_id %>
  </div>
  <div class="field">
    <%= f.label :session_id %><br>
    <%= f.text_field :session_id %>
  </div>
  <div class="field">
    <%= f.label :semester_id %><br>
    <%= f.text_field :semester_id %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.text_field :course_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
